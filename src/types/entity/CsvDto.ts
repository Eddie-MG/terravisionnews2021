export interface ContinentCsv { Id: string; 'Human development groups': string, HDI: string, 'Life_ expectancy': number, 'Mean years of schooling': number }[]
export interface HDICsv { Id: string; Country: string, 'HDI Rank': number, HDI: string, 'Life expectancy': number, 'Mean years of schooling': number, 'Gross national income (GNI) per capita': number, 'GNI per capita rank minus HDI rank': number, 'Change in HDI rank 2010-2015': number }
export interface GEOJson { type: 'FeatureCollection'; 'features': { type: 'Feature'; properties: { scalerank: number; sovereignt: string; level: number; type: string; geounit: string; subunit: string; name: string; name_long: string; economy: string; continent: string; pop_est: number; gdp_md_est: number; income_grp: string }; geometry: any }[] }
